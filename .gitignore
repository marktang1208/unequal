node_modules/
dist/
.wrangler/
.dev.vars
.env
.env.local
*.log
.DS_Store
coverage/
.worktrees/
.claude/worktrees/
.claude/plans/
.superpowers/

# Miniflare integration test build output
# (apps/api/test/integration.test.ts bundles src/index.ts here at suite-setup
# so Miniflare can resolve node_modules-relative bare specifiers.)
apps/api/.test-bundle/---
push/
apps/api/functions/
apps/api/dist/

apps/api/dist/
cloudbaserc.smoke.json
cloudbaserc.smoke.*.json

# Miniprogram cloudfunctions dir: holds a copy of the api-router bundle (14.5MB)
# that wx devtools expects to exist locally for `wx.cloud.callFunction` upload.
# Real deployment is on CloudBase side, so this is a local-build artifact only.
# Phase 7 added the dir; CP-6.5 added the gitignore rule.
apps/miniprogram/cloudfunctions/
cloudbaserc.fn-secrets.json
cloudbaserc.fn-all.json

# CP-7-C #2 deployment artifacts (contain INGEST_PROXY_SECRET / ADMIN_TOKEN plaintext)
ac-verify-cp7c2.sh
cloudbaserc.smoke.cp7c2.json
apps/admin/.tmp/
