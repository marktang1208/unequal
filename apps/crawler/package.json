{
  "name": "crawler",
  "version": "0.1.0",
  "private": true,
  "description": "unequal webpage crawler (curl + cheerio)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "cheerio": "1.0.0"
  },
  "devDependencies": {
    "typescript": "5.5.4",
    "vitest": "2.0.5"
  }
}
